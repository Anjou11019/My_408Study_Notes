##### 9.23
**总分**：==5分==
**注意事项**：
 1. 操作系统大题集中于2.3小节**同步与互斥**、**第三章内存管理**、**4.1文件物理结构**、**5.3磁盘**
 2. 真题考过*2.2CPU调度和5.2设备独立性软件*，并不具有代表性

# 第一章 计算机系统概述
[[操作系统/操作系统强化/第一章 计算机系统概述Q|第一章 计算机系统概述Q]]

**思维导图**：
 - 操作系统的基本概念
    - 管理软硬件资源
        - 处理机管理
        - 存储器管理
        - 文件管理
        - 输入输出设备管理
    - 操作系统的特征
	    - **并发**
	       - 微观分时交替执行，甘特图体现
	       - 区分于并行
	    - **共享**
	       - 互斥共享方式 -- 2.3小节软硬件实现互斥和信号量实现互斥
	       - 同时访问方式
	       - 类似于5.1小节的独占设备和共享设备
	    - **虚拟**
	       - **时分复用**：虚拟处理器
	       - **空分复用**：虚拟存储器
	       - **SPOOLing技术**：独占设备改为共享
	    - **异步**
	       - 多个进程以**不可预知的**速度向前推进
	       - 因为有异步，所以2.3要实现进程同步满足特定需求
 - 操作系统发展历程（不提供人机交互能力）
    - 手工操作阶段：人工干预无操作系统
    - 批处理阶段
       - 单道批处理
       - 多道批处理
    - 分时操作系统
    - 实时操作系统
    - 个人操作系统
 - 操作系统的运行环境
    - 处理器运行模式
       - 用户程序 -- 非特权指令 -- **用户态**
       - 内核程序 -- 特权指令 -- **内核态**
       - **常见非特权指令**：普通运算指令、trap指令
        **常见特权指令** ：IO指令、关中断指令等
    - *内核的四方面的功能*
       - 时钟管理
       - 中断机制
       - 原语
       - 进程管理、存储器管理、设备管理
    - 中断和异常的定义以及分类
       - **异常**：==内中断==，和正在执行的指令**有关**
          - 故障
             - 缺页
             - 除数为0
          - 自陷
          - 终止
       - **中断**：==外中断==，和正在执行的指令**无关**
        常见中断：IO中断，时钟中断、电源掉电 
           - 可屏蔽中断
           - 不可屏蔽中断
    - 系统调用
 - 操作系统结构
 - 操作系统引导
 - 虚拟机


# 第二章 进程与线程
[[第二章 进程与线程Q]]

**思维导图**
 - 进程与线程
    - 进程与线程的基本概念
        - 进程
           - 包含程序段、数据段、进程控制块PCB
           - 资源分配的基本单位
        - 线程
           - 独立调度的基本单位，可访问其隶属进程的资源
           - 线程控制块TCB
    - <span style="color:red;font-weight:bold">进程的状态与转换</span>
       - 就绪态->运行态：就绪的进程被调度，**获得CPU资源**
       - 运行态->就绪态：时间片用完或者被**更高优先级**的进程抢占
       - 运行态->阻塞态：需要的资源**无法申请**时或者**IO操作**开始
       - 阻塞态->就绪态：**IO操作完成**
       - 线程的状态与转换过程和上述基本相同
    - *进程控制：进程的创建、进程的终止、进程的阻塞和唤醒*
      *线程的组织与控制：线程控制块、线程的创建、线程的终止*
    - 进程间通信
       - 共享内存
       - 消息传递
       - 管道
       - 信号
    - 线程的实现级方式
       - 用户级线程
          - 用户视角能看到的线程
          - 线程管理（创建、撤销、切换）由应用程序在**用户态**完成，无需操作系统干预
       - 内核级线程：线程的所有工作在内核空间内（内核态）实现
 - CPU调度
    - 调度的基本概念：从就绪队列，按照某种规则选择一个进程并将CPU分配给该进程运行
    - 调度的实现
        - *调度程序*：未考过
        - *调度的时机、切换与过程*
        - 进程调度的方式
           - 非抢占调度方式
           - 抢占调度方式
        - **闲逛进程**：闲逛进程不需要CPU之外的资源，它不会被阻塞，**优先级最低**
           - 目的是让CPU持续工作，防止空闲
           - 没有就绪进程，则调度闲逛进程；只要有其他进程就绪，就立即让出CPU
    - 调度的目标，常见评价指标
      *常考计算题，较简单需画甘特图分析* 
        - CPU利用率：$\dfrac{CPU有效工作时间}{总时间}$
        - 周转时间：完成时间 - 提交时间
        - 平均周转时间
        - 带权周转时间：$\dfrac{周转时间}{运行时间}$
        - 平均带权周转时间
        - 等待时间：进程等待CPU的时间之和
        - 响应时间：从用户提交请求到系统首次产生响应（即首次执行）所用的时间
    - *进程切换*：上下文及其切换机制 
    - <span style="color:red;font-weight:bold">CPU调度算法</span>
      重点掌握、需结合书上概念以及真题学习
        - 先来先服务调度算法
        - 短作业优先调度算法
        - 高响应比优先调度算法
           - $响应比=\dfrac{等待时间+执行时间}{执行时间}$
        - 优先级调度算法
           - 静态优先级
           - 动态优先级
           - 进程优先级设置依据
              - 系统进程 > 用户进程
              - 交互性进程 > 非交互性进程
              - IO进程 > 计算型进程
        - 时间片轮转调度算法
        - 多队列调度算法
        - 多级反馈调度算法
           - 不同队列的优先级不同，第1级最高
           - 各个队列时间片大小各不相同，优先级越高的队列，时间片越小
           - 进程未执行完时依次被降级到下一级队列
           - 前面所有队列进程为空时，才会调度后续队列
        - ==书表格2.4，常见进程调度算法的特点对比==
    - 多处理器调度（大纲有，未考过）
 - <span style="color:red;font-weight:bold">同步与互斥</span>
    - 同步与互斥的基本概念
        - **临界资源**
           - 一次仅允许一个进程使用的资源称为临界资源
           - 临界区：访问临界资源的代码称为临界区，临界需互斥访问
        - **同步**
           - 进程之间按照想要的顺序执行，==区别于异步（以不可预知的顺序推进）==
        - **互斥**：不能同时发生
        - **实现临界区互斥需要遵循的准则**
            - 空闲让进
            - 忙则等待
            - 有限等待
            - 让权等待（==非必须遵循==）
               - 记录型信号量可以实现让权等待，P操作中有block语句
               - 软件实现互斥的4种方法
               - 硬件实现互斥的3种方法
               - 整型信号量，均不能实现让权等待
    - 互斥的基本实现方法
      结合代码理解，代码难度不高，理解为主
        - **软件方法**
           - 单标志法
              - 两个进程必须交替进入临界区，有可能未被**空闲让进**
           - 双标志先检查法
              - 无需交替进入，但可能同时进入临界区，可能违背**忙则等待**
              - 先while循环检查对方标志位，再修改自己标志位
           - 双标志后检查法
              - 可能都进不了临界区，可能违背**空闲让进和有限等待**
              - 先修改自己的标志位，再while循环检查对方标志位
           - 皮特森算法
              - 结合单标志法和双标志后检查法，遵循空闲让进，忙则等待，有限等待，未遵循让权等待
        - **硬件方法**
           - 中断屏蔽方法：关中断限制了并发能力，不适用于多处理器系统
           - TestAndSet指令（简称TS指令）
           - Swap指令
           - TS指令和Swap指令思想类似，并通过一个共享变量lock
            进入临界区检查lock的值，并且修改lock的值为true
            若修改之前lock为false，则进入临界区
            若修改之前lock为true，则执行循环忙等
            进程退出临界区时，将lock修改为false 
    - 信号量
        - 整型信号量
          结合wait和signal代码理解
            - 信号量是整数，只是用来反映资源的数量
            - 未遵循让权等待
        - 记录型信号量
          结合wait和signaldiamond理解
            - 记录型信号量是一个单独的结构体类型：内部含有信号量的值以及因为该信号量阻塞的进程链表
            - 数值含义：记录型信号量有数值，并且有含义，整数代表可用的资源数量，负数代表阻塞的进程数量
            - wait：数值减1，随后如果<0，则阻塞进程，故可以实现让权等待
            - signal：数值加1，随后如果<=0，则唤醒对应进程
            - <span style="color:red;font-weight:bold">记录型信号量的应用</span>（以下是整个PV大题的基础，PV操作大题的本质是分析实现同步互斥的关系）
                - 利用信号量实现进程互斥
                - 利用信号量实现进程同步
                - 利用信号量实现前驱关系：本质就是同步
        - 让权等待（非必须遵循）
          记录型信号量**可以实现**让权等待，P操作中有block语句
          软件实现互斥的四种方法，硬件实现互斥的三种方法，以及整型信号量均**不能**实现让权等待
    - <span style="color:red;font-weight:bold">经典同步问题</span>
      需熟练掌握代码，**关注信号量的个数、含义作用、初始值、位置**
      **理解P和V的阻塞和唤醒流程**
        - **生产者消费者问题**
        - **哲学家进餐问题**
        - **读者写者问题**
    - 锁
    - 条件变量
       - 区别于记录型信号量
       - 条件变量的wait/signal操作类似于信号量的P/V操作，可以实现进程的阻塞/唤醒
       - 条件变量是**没有数值**的
          - 记录型信号量**有数值**，并且数值有含义，整数代表可用的资源数量，负数代表阻塞的进程数量
 - <span style="color:red;font-weight:bold">死锁</span>
    - 死锁的基本概念
       - 多个进程因竞争资源而造成的一种僵局（互相等待对方手里的资源），使得各个进程都被阻塞，各个进程均无法向前推进
       - <span style="color:red;font-weight:bold">死锁的四个必要条件</span>
          - **互斥**
          - **不可剥夺**
          - **请求和保持**
          - **循环等待**
    - 死锁预防 -- **破坏四个必要条件之一**
       - **破坏互斥**
          - 互斥资源改造为允许共享使用
       - **破坏不可剥夺**
          - 请求新资源得不到满足，释放已有资源，以后需要时再重新申请
       - **破坏请求和保持条件**
          - 一次性申请完全部资源
          - 分配运行需要的部分资源，待全部释放后，再请求新的资源
       - **破坏循环等待条件**
          - 对各类资源编号，每个进程必须按照编号递增的顺序请求资源
    - 死锁避免
       - 基本概念
          - 安全序列：系统按照某种进程推进顺序，使每个进程都可顺利完成
          - 安全状态：能找到一个安全序列
          - 不安全状态：无法找到安全序列
          - 安全状态一定不会死锁，不安全状态可能死锁，发生死锁一定处于**不安全状态**
       - 银行家算法（常规选择计算）
          - 资源总量
          - 资源可利用数量
          - 资源最大需求数量
          - 资源已经分配数量：资源总量 - 已分配数量
          - 资源尚需分配数量：资源最大需求量 - 已分配数量
          - 通过将资源可利用数量与每个进程的资源尚需分配数量对比，求安全序列
            *进程执行完需归还已分配的资源数量，资源可利用数量会跟着变多*
    - 死锁的检测和解除
       - 死锁检测
          - 资源分配图的画法
          - 资源分配图的简化过程
          - 资源分配图简化后，还有边相连的进程就是死锁进程
       - 死锁解除
          - 资源剥夺法
             - 把部分死锁进程的资源剥夺并再分配
          - 撤销进程法
             - 终止进程并回收资源
          - 进程回退法
             - 将一个或多个死锁进程回退到之前的状态，需要系统保持进程的历史信息，设置还原点
# 第三章 内存管理
[[第三章 内存管理Q]]

**思维导图**：
 - 内存管理概念
    - 内存管理的基本概念
       - 逻辑地址与物理地址的概念
          - 逻辑地址
             - 进程具有从0开始连续编号的地址，不同进程可以有相同的逻辑地址
          - 物理地址
             - 内存的从0开始的连续编号的地址，不同进程相同的逻辑地址可以映射到主存不同的物理地址
          - 操作系统通过内存管理软件（MMU）将进程使用的逻辑地址转换为物理地址
       - 程序的链接与装入
          - 编译：将源码编译为若干目标模块
            链接：将多个目标模块，以及需要的库函数链接一起，形成完整的装入模块
            装入：将装入模块放入内存
          - 装入方式（==图3.2==）
             - 绝对装入
                - 程序的逻辑地址与实际内存物理地址完全相同，只适用于单道程序
             - 可重定位装入（==静态重定位==）
                - 装入时将程序中的逻辑地址转换为物理地址，运行期间不能在内存移动
             - 动态运行时装入（==动态重定位==）
                - 程序装入内存时，程序中依然保存的是相对地址，运行时才把程序中的地址转为绝对地址，程序在内存中可发生移动
          - 链接方式
             - 静态链接：程序运行之前，先将各个目标模块连接成完整的装入模块
             - 装入时动态链接：边装入边链接
             - 运行时动态链接：不需要的目标模块先不调入内存，执行时要用到再链接
       - 进程的内存映像
          - 操作系统内核区
          - 用户栈区：函数调用相关信息，函数中的局部变量
          - 共享库的存储映射区：printf函数的代码
          - 堆区：malloc申请的空间
          - 读/写数据：全局变量和静态变量
          - 只读代码：程序对应的机器指令
       - 内存保护
          - 确保每个进程都有一个单独的物理地址空间，进程在运行时的地址访问范围不能超过自己的物理地址空间范围
       - 内存共享
          - 内存映像中的只读代码
          - 可重入代码：允许多个进程同时访问但不允许任何进程修改
       - 内存分配与回收
    - 连续分配管理方式
       - 单一连续分配
          - 内存中仅有一个程序，==有内部碎片，无外部碎片==
       - 固定分区分配
          - 将内存划分为若干固定大小的分区，每个分区存放一个程序
          - 分区大小相等
          - 分区大小不等
          - ==有内部碎片，无外部碎片==
       - 动态分区分配
          - 根据进程实际需要，动态为其分配内存，分区大小等于进程需要的大小
          - ==无内部碎片，有外部碎片==，需要通过紧凑技术，对进程移动和整理
       - 动态分区分配算法
          - 首次适应算法
             - 空闲分区按**地址递增**进行分配
          - 临近适应算法
             - 分配内存时从上次查找结束的位置开始继续查找
          - 最佳适应算法
             - 空闲分区按容量递增进行分配，**性能查，留下很小的难以利用的块，产生==最多==的外部碎片**
          - 最坏适应算法
             - 空闲分区按**容量递减**进行分配
    - <span style="color:red;font-weight:bold">非连续分配管理方式</span>
      概念很多，涉及很多计算
        - <span style="color:red;font-weight:bold">页式管理</span>（==重要，多看书，多总结，多做题==）
           - 页面和页框
            页号和页框号
           - 逻辑地址结构：页号+页内偏移量
           - 页表（页表也在内存）
              - 每个进程有一张自己的页表
              - 页表项构成：**页号+页框号**，页号是隐含的
              - 页表的作用：实现页号到物理块号的地址映射
              - 根据页表起始物理地址和页号，计算其对应页表项的物理地址
                类比根据数组首地址和数组下标，计算数组某一个元素的地址
           - 地址变换流程（==图3.9==）
              - 根据页表项物理地址访问页表
              - 根据地址变换后的物理地址，访问需要的数据
              - 两次访存
           - 页表寄存器（PTR）
              - 存放页表起始物理地址和页表长度
              - 页表起始物理地址用于计算任一个页表项的物理地址
              - 页表长度用于判断地址越界
           - 地址变换流程的十进制分析
           - 思考：如何确定页表项的大小
           - 具有块表的地址变换流程，需结合==**计组3.6**==，结合TLB映射方式进行查找快表
           - 两级页表（==综合性较强==）
              - 逻辑地址结构：页目录号+页号+页内偏移
              - 页目录表（外层页表）、页目录项、页目录号（一级页号）
              - 页表（二级页表）、页表项、页号（二级页号）
              - 页目录表的页目录项中存放的是二级页表所在的页框号
              - 页目录表和二级页表的表项物理地址计算
              - 在采用二级页表的分页系统中，CPU页表基址寄存器存放的是当前进程的一级页表的起始物理地址
              - 多级页表的优点：减少页表所占的连续内存空间
              - 地址变换过程
                 - 根据页目录号访问页目录表
                 - 根据页号访问页表
                 - 根据页表中的页框号和页内偏移量拼接后的物理地址，访问需要的数据
                 - 三次访存
        - 段式管理
        - 段页式管理（==未考过，综合性强==）
 - 虚拟内存管理
# 第四章 文件管理
[[第四章 文件管理Q]]
# 第五章 输入输出系统
[[第五章 输入输出系统Q]]